import React, { FC } from 'react';
import styles from './OrderElement.module.css'

const OrderElement: FC = (): React.ReactElement => {
  return (
    <div>

    </div>
  )
}

export default OrderElement
